#include <stdio.h>
#include <stdlib.h>

void bin_dec(){
    int esp, mantissa; // esponente e mantissa nel numero macchina
    int decisione = 1;
    int num, lenght;
    int * arr; 
    printf("Type number of cifre for exp: "); 
    scanf("%d", &esp);
    printf("Type number of mantissa: ");
    scanf("%d", &mantissa);

    
        while (decisione == 1){
            printf("Type binary number: ");
            scanf("%d", &num);
            while (num > 0){
                print("hello world");
            }
        }
}